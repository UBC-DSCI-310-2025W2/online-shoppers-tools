name: shopperspkg
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip = 26.0.1
  - pytest = 9.0.2

  # core data + viz
  - numpy = 2.2.6
  - pandas = 2.3.3

  # ML
  - scikit-learn = 1.7.2
  - matplotlib = 3.10.8
  - click = 8.3.1

  # SHAP often easiest via pip
  - pip:
      - shap == 0.49.1
